# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
development:
  adapter: mysql
  host: localhost
  database: lrm_development
  username: root


  
test:
  adapter: mysql
  host: localhost
  database: lrm_test
  username: root